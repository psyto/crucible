{
  "name": "@crucible/core",
  "version": "0.1.0",
  "description": "Core types, scoring engine, and adapter interface for Crucible",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
